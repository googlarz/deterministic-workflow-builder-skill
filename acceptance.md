# Acceptance

- Run `python3 -m unittest discover -s tests -p 'test_*.py'`
- Run `python3 scripts/evaluate_benchmarks.py`
- Run `python3 scripts/verify_workflow.py <workflow-dir> --simulate`
- Run `./run_workflow.sh --doctor` after an interrupted or failed run
